```ts title="SomeButton.ts"
const SomeButton = ma.button<{ $isLoading?: boolean }>`
  text-normal
  md:text-lg
  mt-5
  border-1
  transition-all
  ${someConfig.transitionDurationEaseClass}
  ${({ $isLoading }) => $isLoading && "opacity-90 pointer-events-none"}
`;
```
